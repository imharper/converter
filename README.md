# 📁 Веб-приложение для конвертации файлов

Современное веб-приложение для конвертации различных типов файлов, включая изображения, PDF и DOCX документы.

## 🌟 Возможности

### 🖼️ Конвертация изображений
- Поддерживаемые входные форматы: JPEG, PNG, GIF, BMP, TIFF, WebP
- Выходные форматы: PNG, JPEG, WebP, AVIF
- Настройка качества сжатия
- Drag & Drop интерфейс

### 📄 Конвертация PDF
- **PDF → Изображения** (PNG, JPEG) с высоким качеством (300 DPI)
- **PDF → DOCX** - извлечение текста и создание Word документа
- Обработка многостраничных документов
- Скачивание отдельных страниц (для изображений)

### 📝 Конвертация DOCX
- **DOCX → HTML** с сохранением форматирования
- **DOCX → TXT** - извлечение обычного текста
- **DOCX → PDF** - высококачественная конвертация с сохранением стилей
- Обработка предупреждений конвертации

## 🛠️ Технологии

### Backend
- **Node.js** + **Express.js** - серверная часть
- **Multer** - загрузка файлов
- **Sharp** - обработка изображений
- **pdf2pic** - конвертация PDF в изображения
- **pdf-parse** - извлечение текста из PDF
- **Mammoth** - конвертация DOCX файлов
- **docx** - создание DOCX документов
- **Puppeteer** - конвертация HTML в PDF

### Frontend
- **React** - пользовательский интерфейс
- **Material-UI** - компоненты UI
- **React Dropzone** - drag & drop функциональность
- **Axios** - HTTP клиент

## 📦 Установка и запуск

### Предварительные требования
- Node.js (версия 14 или выше)
- npm или yarn
- Google Chrome или Chromium (для PDF генерации)

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd file-converter-web-app
```

### 2. Установка зависимостей
```bash
# Установка зависимостей сервера
npm install

# Установка зависимостей клиента
cd client
npm install
cd ..
```

### 3. Запуск приложения

#### Режим разработки
```bash
# Запуск сервера и клиента одновременно
npm run dev
```

#### Запуск отдельно
```bash
# Запуск только сервера (порт 5000)
npm run server

# Запуск только клиента (порт 3000)
npm run client
```

### 4. Открытие приложения
- Frontend: http://localhost:3000
- Backend API: http://localhost:5000

## 📁 Структура проекта

```
file-converter-web-app/
├── client/                 # React frontend
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   │   ├── ImageConverter.js
│   │   │   ├── PdfConverter.js
│   │   │   └── DocxConverter.js
│   │   ├── App.js         # Главный компонент
│   │   └── App.css        # Стили
│   └── package.json       # Зависимости клиента
├── server.js              # Express сервер
├── package.json           # Зависимости сервера
├── uploads/               # Временные загруженные файлы
├── converted/             # Сконвертированные файлы
└── README.md              # Документация
```

## 🔧 API Endpoints

### POST /api/convert/image
Конвертация изображений
- **Body**: FormData с файлом, форматом и качеством
- **Response**: URL для скачивания сконвертированного файла

### POST /api/convert/pdf-to-image
Конвертация PDF в изображения
- **Body**: FormData с PDF файлом и форматом
- **Response**: Массив URL для скачивания страниц

### POST /api/convert/pdf-to-docx
Конвертация PDF в DOCX
- **Body**: FormData с PDF файлом
- **Response**: URL для скачивания DOCX файла + статистика извлечения

### POST /api/convert/docx-to-pdf
Конвертация DOCX в PDF
- **Body**: FormData с DOCX файлом
- **Response**: URL для скачивания PDF файла

### POST /api/convert/docx
Конвертация DOCX файлов
- **Body**: FormData с DOCX файлом и форматом вывода (html/txt)
- **Response**: URL для скачивания сконвертированного файла

### GET /api/download/:filename
Скачивание файла
- **Params**: filename - имя файла для скачивания

## 🎯 Использование

1. **Выберите тип конвертации** в соответствующей вкладке
2. **Перетащите файл** в область загрузки или нажмите для выбора
3. **Настройте параметры** конвертации (формат, качество)
4. **Нажмите "Конвертировать"** и дождитесь завершения
5. **Скачайте результат** по предоставленной ссылке

## 🔄 Поддерживаемые конвертации

### Изображения
- JPEG ↔ PNG ↔ WebP ↔ AVIF ↔ GIF ↔ BMP ↔ TIFF

### PDF
- PDF → PNG/JPEG (страницы как изображения)
- PDF → DOCX (извлечение текста)

### DOCX
- DOCX → HTML (с форматированием)
- DOCX → TXT (обычный текст)
- DOCX → PDF (высокое качество)

## 🔒 Ограничения

- Максимальный размер файла: 50 МБ
- Поддерживаемые форматы изображений: JPEG, PNG, GIF, BMP, TIFF, WebP
- Поддерживаемые документы: PDF, DOCX

## 🛡️ Безопасность

- Автоматическая очистка временных файлов
- Проверка типов файлов
- Ограничение размера загружаемых файлов
- Изолированная обработка в headless браузере

## 📝 Лицензия

MIT License

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас есть вопросы или проблемы, создайте issue в репозитории. 